name: food_delivery_app_301
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  #DI
  get_it: ^7.7.0

  #STATE_MANAGEMENT
  stacked: ^3.4.2
  stacked_services: ^1.5.0

  #API
  dio: ^5.4.3+1  

dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^3.0.0
  build_runner:
  stacked_generator: ^1.6.0


flutter:

  
  uses-material-design: true

  assets:
     - assets/

  